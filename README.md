# 🛡️ DriverSight v1.2

**DriverSight** — это профессиональный инструмент для аудита поверхности атаки ядра Windows. Разработан для специалистов **Red Team** с целью выявления уязвимых драйверов, пригодных для проведения атак класса **BYOVD** (Bring Your Own Vulnerable Driver).

Программа позволяет обнаружить легитимные, но подписанные драйверы с известными CVE, которые могут быть использованы для повышения привилегий до уровня ядра (Ring 0) или отключения средств защиты (EDR/AV).

---

## ✨ Ключевые особенности

*   **Stealth WinAPI Discovery:** Инструмент не использует системные утилиты (`driverquery`, `sc`), которые легко мониторятся EDR. Вместо этого реализован прямой вызов функций ядра через `psapi.dll` (`EnumDeviceDrivers`).
*   **Advanced CLI (UX):** Интерфейс на базе библиотеки `rich` с анимированными индикаторами прогресса, статус-барами и наглядными таблицами.
*   **Threat Intelligence Auto-Update:** Встроенный модуль синхронизации с базой [LOLDrivers API](https://loldrivers.io). База перестраивается в локальную хеш-таблицу для мгновенного поиска (O(1)).
*   **Red Team Priority Scoring:** Каждый найденный драйвер оценивается по степени полезности для атакующего (например, возможность записи в память ядра или обхода SMEP).
*   **Professional Reporting:** Автоматическая генерация интерактивного HTML-отчета в стиле "Dark Mode" и детализированная таблица в консоли.

---

## 📂 Структура проекта

```text
DriverSight/
├── core/
│   ├── collector.py  # Stealth-сборщик (WinAPI)
│   ├── hasher.py     # Криптографический модуль (SHA-256)
│   ├── analyzer.py   # Скоринг и сопоставление с БД
│   ├── updater.py    # Синхронизация с облачной базой LOLDrivers
│   └── engine.py     # Оркестратор логики сканирования
├── utils/
│   ├── interface.py  # Визуальные компоненты (баннеры, стили)
│   └── reporter.py   # Генератор консольных и HTML отчетов
├── main.py           # Точка входа (CLI Parser)
└── requirements.txt  # Зависимости проекта
```

---

## 🛠 Установка и запуск

### Предварительные условия
*   Windows 10 / 11 (x64)
*   Python 3.10 или выше
*   Права администратора (необходимы для доступа к объектам драйверов в памяти)

### Инсталляция
1. Склонируйте репозиторий.
2. Установите необходимые зависимости:
```bash
pip install -r requirements.txt
```

### Использование
**1. Обновление базы сигнатур (рекомендуется перед каждым сканированием):**
```bash
python main.py --update
```

**2. Запуск полного аудита системы:**
```bash
python main.py
```

---

## 📊 Маппинг на тактики MITRE ATT&CK®

*   **Discovery (T1652):** Device Driver Discovery — сбор информации о загруженных модулях ядра.
*   **Privilege Escalation (T1068):** Exploitation for Privilege Escalation — поиск векторов для получения прав SYSTEM.
*   **Defense Evasion (T1562.001):** Impair Defenses — выявление драйверов для терминации процессов антивирусного ПО.

---

## 📄 Выходные данные

1.  **Console Table:** Мгновенный вывод критических находок с цветовой кодировкой (Красный — критично, Желтый — средний риск).
2.  **DS_Report.html:** Подробный отчет со ссылками на PoC (Proof of Concept) эксплойты для каждого найденного драйвера.

---

## ⚠️ Отказ от ответственности (Disclaimer)
Данное программное обеспечение предназначено исключительно для целей образовательного тестирования на проникновение и легитимного аудита безопасности. Использование DriverSight против целей без предварительного разрешения является незаконным.

---

### Рекомендация для защиты проекта:
Если ты будешь презентовать проект, обязательно упомяни, что **версия 1.2** была полностью переработана для обеспечения модульности (принципы SOLID) и улучшения пользовательского опыта (UX), что делает её полноценным инструментом, а не просто скриптом.